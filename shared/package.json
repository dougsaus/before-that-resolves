{
  "name": "@before-that-resolves/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Before That Resolves",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2"
  }
}