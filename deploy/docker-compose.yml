services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: btr
      POSTGRES_PASSWORD: btr
      POSTGRES_DB: btr
    ports:
      - "5432:5432"
    volumes:
      - btr_postgres:/var/lib/postgresql/data

volumes:
  btr_postgres:
